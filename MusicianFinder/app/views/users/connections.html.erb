<div class="panel panel-default">
  <div class="panel-body">
      <div class="content">
       <% if user_signed_in? %>
            <% if @user.id == current_user.id %>
          <% if @user.requested_friends.size > 0 %>
          <h4 class="pendingH">PENDING CONNECTION REQUESTS</h4>
            <hr>
          <% @user.requested_friends.each do |friend| %>
                  <div class="thumbnail">
                    
                      <%= link_to (image_tag friend.image.url(:thumb)), friend %>
                          <h5 class="userMain"><%= friend.username %></h5>
                          
                          <div class="btn-group">
                              <%= link_to user_accept_path(friend), class: "btn btn-default btn-sm" do %>

    <span class="glyphicon glyphicon-ok"></span>
    Accept

  <% end %>
            
                              <%= link_to "#", class: "btn btn-default btn-sm" do %>

    <span class="glyphicon glyphicon-remove"></span>
    Decline
                             
                              <% end %>
                              
                              
                    




      </div>
                        
                          
                  </div>
                  <% end %>
          <% end %>
            <% end %>
        <% end %>
        
          <h4 class="connectionH"><%= @user.username %>'s connections</h4>
          <hr>
          <div class="friendList">
          <ul class="friends">
          <% @user.friends.each do |friend| %>
          <li>
           <div class="thumbnail">
          <%= link_to (image_tag friend.image.url(:thumb)), friend %>
          <h5 class="userMain"><%= friend.username %></h5>
          </div>
          </li>
           
          <% end %>
          </ul>
          </div>
          
          <div class="col-sm-4">
  	<div class="item">
  		<a href="#">
			<span class="notify-badge">NEW</span>
      		<img src="http://placehold.it/200x200"  alt="" />
		</a>
	</div>
</div>

       
        
        
        </div>
    </div>
</div>


