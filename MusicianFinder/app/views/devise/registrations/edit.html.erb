<div class="formBoxEdit">
<h2 class="regHead">Edit <%= resource_name.to_s.humanize %></h2>


<%= form_for(resource, as: resource_name, url: registration_path(resource_name),:html => { :multipart => true }) do |f| %>
  <%= devise_error_messages! %>



  <div class="form-group">
    <%= f.label :email %><br />
    <%= f.email_field :email, autofocus: true, class: "form-control" %>
  </div>

  <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
    <div>Currently waiting confirmation for: <%= resource.unconfirmed_email %></div>
  <% end %>

  <div class="form-group">
      <%= f.label :username %><br />
      <%= f.text_field :username, autocomplete: "off", class: "form-control" %>
  </div>

  <div class="form-group">
	  	<%= f.label :location %>
          <%= f.collection_select :location, Location::Cities, :to_s, :to_s,
:include_blank => true, class: "form-control" %>
	  </div>

    <div class="form-group">
        <%= f.label :about %><br />
        <%= f.text_area :about, placeholder: "About yourself", autocomplete: "off", class: "form-control" %>
    </div>

  <div class="form-group">
      <%= f.label :instruments %><br />
      <%= f.text_field :instruments, placeholder: "Instruments you play", autocomplete: "off", class: "form-control" %>
  </div>

  <div class="form-group">
      <%= f.label :influences %><br />
      <%= f.text_area :influences, placeholder: "Influences", autocomplete: "off", class: "form-control" %>
  </div>

  <div class="form-group">
      <%= f.label :experience %><br />
      <%= f.text_area :experience, placeholder: "Prior experience", autocomplete: "off", class: "form-control" %>
  </div>

  <div class="form-group">
      <%= f.label :gear %><br />
      <%= f.text_area :gear, placeholder: "Gear you use", autocomplete: "off", class: "form-control" %>
  </div>

  <div class="form-group">
      <%= f.label :media %><br />
      <%= f.text_field :media, placeholder: "Paste in embed url here", autocomplete: "off", class: "form-control" %>
  </div>

  <div class="form-group">
    <%= f.label :password %> <i>(leave blank if you don't want to change it)</i><br />
    <%= f.password_field :password, autocomplete: "off", class: "form-control" %>
  </div>

  <div class="form-group">
    <%= f.label :password_confirmation %><br />
    <%= f.password_field :password_confirmation, autocomplete: "off", class: "form-control" %>
  </div>

  <div class="form-group">
    <%= f.label :current_password %> <i>(we need your current password to confirm your changes)</i><br />
    <%= f.password_field :current_password, autocomplete: "off", class: "form-control" %>
  </div>

  <div class="form-group">
      <%= f.label :image %><br />
      <%= f.file_field :image%>
  </div>



  <%= f.submit 'Update', :class => 'btn btn-block btn-primary' %>


<% end %>

<div class="form-group">

<p>Unhappy? <%= button_to 'Delete my account', registration_path(resource_name), data: { confirm: "Are you sure?" }, method: :delete, :class => 'btn btn-sm btn-danger' %></p>

<br><br>
<%= button_to "Back", :back, :class => 'btn btn-sm btn-primary' %>
</div>
</div>
